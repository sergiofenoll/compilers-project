#include <stdio.h>


int fib(int n) {
    int f = 1;
    if (n > 2) {
        //f = fib(n - 2) + fib(n - 1);
        int a;
    }

    printf("%d", f);
    return f;
}


int main() {
    int n = 4;
    fib(n);
    return 0;
}
