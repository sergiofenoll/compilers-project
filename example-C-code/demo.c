#include <stdio.h>

int main() {

    int a = 5 + 5;
    int b = a * 2;

    if (a > 5) {
        b = 5;
    }

    for (int c = 3; c > 0; c -= 1) {
        b = a + 5;
        continue;
        int j = 3 + 2 * 1;
    }
    
    int d = 5;
    char ch = 'n'; 
    if (d > 5) {
        ch = 't';
    } else {
        ch = 'f';
    }
    printf("%c", ch);

    return 0;

    float x = 4.0;
    float y = x + 1.0;

}
